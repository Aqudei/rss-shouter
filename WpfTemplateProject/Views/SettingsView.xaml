<UserControl x:Class="RSSLoudReader.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RSSLoudReader.Views"
             mc:Ignorable="d" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mah:DialogParticipation.Register="{Binding}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel >
            <Label>Delay Interval</Label>
            <TextBox x:Name="DelayIntervalSeconds"/>
            <Button Margin="2,16,2,2" x:Name="Save">Save Settings</Button>
            <Label>Logo</Label>
            <TextBox x:Name="Logo"/>
            <Button x:Name="BrowseLogo" HorizontalAlignment="Right" Margin="2">Browse Logo</Button>
            <Button Margin="2,16,2,2" x:Name="ApplyLogo">Apply Logo</Button>
        </StackPanel>
    </Grid>
</UserControl>
